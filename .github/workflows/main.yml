name: Test workflow

on:
  push:
    branches:
      - 'main'
    tags-ignore:
      - 'v*'

permissions:
  contents: write

jobs:

  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          source .github/bash-functions
          newRelease

  merge-to-dev:
    needs: [release]
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v3
        with:
          ref: 'dev'
      - run: |
          source .github/bash-functions
          merge origin/main
